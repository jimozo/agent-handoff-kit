# AGENTS.md

Read `SESSIONS.md`, then create a branch before editing:

```bash
git checkout main
git pull --ff-only
git checkout -b codex/YYYY-MM-DD-short-scope
```

Use `claude/`, `cursor/`, or `aider/` for other agents.

For handoff rules, see `docs/agent-handoff-kit.md`.
